import XCTest
@testable import hotel_management_cli

final class hotel_management_cliTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(hotel_management_cli().text, "Hello, World!")
    }
}
